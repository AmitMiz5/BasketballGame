<nav>
    <div>
        <NavLink href="" Match="NavLinkMatch.All">
            משחק
        </NavLink>
    </div>
    <div>
        <NavLink href="GamesList">
            עורך
        </NavLink>
    </div>
    <div>
        <NavLink href="About">
            אודות
        </NavLink>
    </div>
</nav>

<style>
    /* Align the navigation bar to the left */
    nav {
        display: flex;
        justify-content: flex-start;
        background-color: #e0e0e0; /* Light gray background */
        padding: 15px;
        width: fit-content; /* Adjusts width to fit content */
        margin-left: 0; /* Aligns nav to the left of the container */
    }

        /* Styling each navigation button */
        nav div {
            margin: 0 10px;
        }

        nav a {
            background-color: #2e6e85; /* Default button color */
            color: #0033cc; /* Dark blue text color */
            padding: 15px 25px;
            text-align: center;
            text-decoration: none;
            border: 2px solid #5c3d53; /* Matches the border color from the image */
            border-radius: 8px; /* Rounded corners */
            font-size: 1.1em;
            font-weight: bold;
            display: inline-block;
            box-sizing: border-box;
            transition: background-color 0.3s ease, transform 0.2s ease; /* Smooth hover effect */
        }

            nav a:hover {
                background: linear-gradient(135deg, #2e6e85, #4a90a4); /* Soft gradient for hover effect */
                color: white; /* Change text color on hover */
                transform: translateY(-2px); /* Slight upward shift on hover */
            }

            nav a:active {
                background-color: #1f4f63; /* Even darker shade for active state */
            }
</style>



@code {

}